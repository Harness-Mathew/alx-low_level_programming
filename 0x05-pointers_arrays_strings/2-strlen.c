#include <string.h>
#include <stdio.h>
/**
 * main -entry point
 * Description: print string length
 * Return: 0
**/
int main(void)
{
	char love[10] = "manly";

	int x = strlen(love);

	printf("%d", x);
	return (0);
}
